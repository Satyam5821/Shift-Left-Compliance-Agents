/** SPA path for workspace picker / GitHub setup (bookmarkable). */
export const PATH_WORKSPACES = '/workspaces'

/** SPA path for in-app login page (open in new tab for demo). */
export const PATH_LOGIN = '/login'
